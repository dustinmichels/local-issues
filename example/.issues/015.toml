[issue]
id = 15
title = "Lazy-Loaded Background Modal (Instant)"
status = "open" # [open, in-progress, done]
assigned_to = "" # [claude, antigravity, ""]
created_at = 2026-06-10

[details]
description = """
Calendly currently opens via its default click handlers. Instead, lazy-load
an inline Calendly widget into a hidden modal so it can be shown instantly
on click rather than loading the widget on demand.
"""

[subtasks]
"Create a hidden modal element on the page and initialize Calendly as an inline widget inside it (lazy-loaded after page load or on first user scroll)" = false
"Replace default Calendly click handlers with a toggle to show/hide this hidden modal, providing an instant popup experience" = false

[resolution]
notes = ""
